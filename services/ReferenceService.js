const Reference = require('../models/Reference'),
    { ObjectId } = require('../config/mongoose').Types;

module.exports = {

    create(ref) {

    },
    update(userId, ref) {

    },
    select(userId) {

    },
    delete(refId){

    }
}